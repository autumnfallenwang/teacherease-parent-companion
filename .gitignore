# ---------- Node / npm / pnpm / yarn ----------
node_modules/
.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.npm/
.eslintcache

# ---------- Next.js ----------
.next/
out/
next-env.d.ts

# ---------- TypeScript build output ----------
dist/
build/
*.tsbuildinfo

# ---------- Rust / Cargo / Tauri ----------
target/
**/target/
*.rs.bk
Cargo.lock.bak
src-tauri/target/
src-tauri/gen/schemas/
src-tauri/WixTools/

# ---------- Tauri bundler output ----------
src-tauri/target/release/bundle/

# ---------- Environment & secrets ----------
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.keystore

# Tauri updater signing keys — never commit the private key
tauri-updater.key
tauri-updater.key.pub
!tauri-updater.pub

# ---------- App data / local databases ----------
*.db
*.db-journal
*.sqlite
*.sqlite3
*.sqlite3-journal
app.db
logs/
*.log

# ---------- Debug HTML dumps from scraper development ----------
debug/
*_debug.html
scraper-fixtures/*.html

# ---------- Reference predecessor project (local-only, not for commit) ----------
# Python + Playwright predecessor, copied here for HTML fixture mining and
# parser reference. See docs/design-plan.md "Predecessor" section.
ref/

# ---------- Sandbox: POCs, live-credential smoke tests, spikes ----------
# Anything that touches real TeacherEase accounts or real PII lives here
# and is NEVER committed. See CLAUDE.md "Security constraints" section.
sandbox/

# ---------- OS ----------
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# ---------- Editors / IDEs ----------
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/
*.swp
*.swo
*~
.project
.classpath
.settings/

# ---------- Claude Code runtime state ----------
.claude/scheduled_tasks.lock

# ---------- Misc ----------
coverage/
.nyc_output/
*.tgz
*.pid
*.seed
